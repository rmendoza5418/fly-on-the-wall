# Core — required
faster-whisper>=1.0.0
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.24
sumy>=0.11.0
nltk>=3.8
Pillow>=10.0         # for scripts/generate_icons.py

# Optional — Ollama abstractive summarization
# Requires Ollama running locally: https://ollama.com
# Recommended models for RTX 4090: llama3.2, qwen2.5:7b, mistral
# Enable in settings: ollamaEnabled = true, ollamaModel = "llama3.2"
# pip install ollama>=0.2.0

# Optional — speaker diarization (requires HuggingFace token)
# pip install pyannote.audio
# See: https://hf.co/pyannote/speaker-diarization-3.1
